{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Deploy with stable URL",
      "type": "shell",
      "windows": {
        "command": "${workspaceFolder}\\catbus_scripts\\deploy.bat"
      },
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}
