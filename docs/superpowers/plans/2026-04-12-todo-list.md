# To-Do List Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a coordinator-facing To-Do List page inside CATBUS for tracking clinic prep and volunteer management deadlines, with escalating automated email reminders at 14, 7, and 1 day(s) before each task's due date.

**Architecture:** A new `?app=todo` CATBUS page served by `TodoListApp.gs`, backed by two new Google Sheets tabs ("Action Items" and "Coordinators") in the existing spreadsheet. Data CRUD and email reminders live in `TodoList.gs`. A daily Apps Script trigger handles reminder sends.

**Tech Stack:** Google Apps Script, Google Sheets, HtmlService, MailApp, `google.script.run` for frontend↔backend calls.

---

## File Map

| File | Action | Purpose |
|---|---|---|
| `catbus_scripts/TodoList.gs` | **Create** | All sheet CRUD + reminder trigger logic |
| `catbus_scripts/TodoListApp.gs` | **Create** | `doGetTodoList()` page server |
| `catbus_scripts/todo_list.html` | **Create** | Full frontend: dashboard, modals, collapsible people section |
| `catbus_scripts/Config.gs` | **Modify** | Add `ACTION_ITEM_CONFIG` and two sheet name constants |
| `catbus_scripts/Router.gs` | **Modify** | Add `case 'todo'` |
| `webpage/admin.html` | **Modify** | Add To-Do List tool card tile |

---

## Task 1: Add Config and Sheet Name Constants

**Files:**
- Modify: `catbus_scripts/Config.gs`

- [ ] **Step 1: Add sheet name constants to `CONFIG.SHEETS`**

Open `catbus_scripts/Config.gs`. Inside the `SHEETS` object (around line 16), add two entries at the end of the block:

```js
ACTION_ITEMS: 'Action Items',
COORDINATORS: 'Coordinators',
```

- [ ] **Step 2: Add `ACTION_ITEM_CONFIG` at the bottom of `Config.gs`**

After all existing config objects, append:

```js
/**
 * Configuration for the coordinator To-Do List feature.
 */
const ACTION_ITEM_CONFIG = {
  REMINDER_THRESHOLDS: [14, 7, 1],   // days before due date (calendar days)
  SHEET_NAME: 'Action Items',
  COORDINATORS_SHEET_NAME: 'Coordinators'
};
```

- [ ] **Step 3: Verify no syntax errors**

In the Apps Script editor (or via `clasp push` then check logs), confirm the script project loads without errors. You can open any other page (e.g., `?app=admin`) and confirm it still works.

- [ ] **Step 4: Commit**

```bash
git add catbus_scripts/Config.gs
git commit -m "feat: add ACTION_ITEM_CONFIG and sheet name constants for To-Do List"
```

---

## Task 2: Create Coordinator Sheet Helpers

**Files:**
- Create: `catbus_scripts/TodoList.gs`

- [ ] **Step 1: Create `catbus_scripts/TodoList.gs` with coordinator sheet helpers**

```js
/**
 * TodoList.gs
 * CRUD for the "Action Items" and "Coordinators" sheets,
 * plus the daily reminder trigger for the To-Do List feature.
 */

// ─── Coordinator helpers ────────────────────────────────────────────────────

/**
 * Returns or creates the "Coordinators" sheet.
 * Columns: Name | Email
 */
function getOrCreateCoordinatorsSheet_() {
  const ss = getSpreadsheet();
  let sheet = ss.getSheetByName(ACTION_ITEM_CONFIG.COORDINATORS_SHEET_NAME);
  if (!sheet) {
    sheet = ss.insertSheet(ACTION_ITEM_CONFIG.COORDINATORS_SHEET_NAME);
    sheet.appendRow(['Name', 'Email']);
    sheet.getRange(1, 1, 1, 2).setFontWeight('bold');
  }
  return sheet;
}

/**
 * Returns all coordinators as an array of { rowIndex, name, email }.
 * rowIndex is 1-based (including header row offset).
 */
function getCoordinators() {
  const sheet = getOrCreateCoordinatorsSheet_();
  const lastRow = sheet.getLastRow();
  if (lastRow < 2) return [];
  const data = sheet.getRange(2, 1, lastRow - 1, 2).getValues();
  return data
    .map((row, i) => ({ rowIndex: i + 2, name: row[0], email: row[1] }))
    .filter(c => c.name || c.email);
}

/**
 * Appends a new coordinator row.
 * @param {Object} data - { name: string, email: string }
 */
function addCoordinator(data) {
  if (!data || !data.name || !data.email) throw new Error('Name and email are required.');
  const sheet = getOrCreateCoordinatorsSheet_();
  sheet.appendRow([data.name.trim(), data.email.trim()]);
}

/**
 * Deletes a coordinator by 1-based sheet row index.
 * @param {number} rowIndex
 */
function deleteCoordinator(rowIndex) {
  const sheet = getOrCreateCoordinatorsSheet_();
  sheet.deleteRow(rowIndex);
}
```

- [ ] **Step 2: Push to Apps Script and verify**

```bash
cd catbus_scripts && clasp push
```

Open Apps Script editor → Run `getCoordinators` → confirm it returns `[]` without errors and that the "Coordinators" sheet was created with a header row.

- [ ] **Step 3: Commit**

```bash
git add catbus_scripts/TodoList.gs
git commit -m "feat: add coordinator sheet helpers (getCoordinators, addCoordinator, deleteCoordinator)"
```

---

## Task 3: Create Action Items Sheet Helpers

**Files:**
- Modify: `catbus_scripts/TodoList.gs`

- [ ] **Step 1: Append Action Items helpers to `TodoList.gs`**

Add the following after the coordinator section:

```js
// ─── Action Item helpers ─────────────────────────────────────────────────────

const ACTION_ITEM_HEADERS_ = [
  'Title', 'Description', 'Category', 'Due Date',
  'Responsible Individuals', 'Status', 'Reminders Sent',
  'Created At', 'Updated At'
];

/**
 * Returns or creates the "Action Items" sheet.
 */
function getOrCreateActionItemsSheet_() {
  const ss = getSpreadsheet();
  let sheet = ss.getSheetByName(ACTION_ITEM_CONFIG.SHEET_NAME);
  if (!sheet) {
    sheet = ss.insertSheet(ACTION_ITEM_CONFIG.SHEET_NAME);
    sheet.appendRow(ACTION_ITEM_HEADERS_);
    sheet.getRange(1, 1, 1, ACTION_ITEM_HEADERS_.length).setFontWeight('bold');
  }
  return sheet;
}

/**
 * Returns all action items as an array of objects.
 * Each object includes `rowIndex` (1-based, offset for header).
 */
function getTodoItems() {
  const sheet = getOrCreateActionItemsSheet_();
  const lastRow = sheet.getLastRow();
  if (lastRow < 2) return [];
  const data = sheet.getRange(2, 1, lastRow - 1, ACTION_ITEM_HEADERS_.length).getValues();
  return data.map((row, i) => ({
    rowIndex:               i + 2,
    title:                  row[0],
    description:            row[1],
    category:               row[2],
    dueDate:                row[3] ? Utilities.formatDate(new Date(row[3]), Session.getScriptTimeZone(), 'yyyy-MM-dd') : '',
    responsibleIndividuals: row[4],
    status:                 row[5],
    remindersSent:          row[6],
    createdAt:              row[7] ? row[7].toString() : '',
    updatedAt:              row[8] ? row[8].toString() : ''
  }));
}

/**
 * Appends a new action item row.
 * @param {Object} data - { title, description, category, dueDate, responsibleIndividuals, status }
 */
function createTodoItem(data) {
  if (!data.title) throw new Error('Title is required.');
  if (!data.dueDate) throw new Error('Due date is required.');
  const sheet = getOrCreateActionItemsSheet_();
  const now = new Date();
  sheet.appendRow([
    data.title.trim(),
    (data.description || '').trim(),
    data.category || 'Clinic Prep',
    new Date(data.dueDate),
    (data.responsibleIndividuals || ''),
    data.status || 'Not Started',
    '',   // Reminders Sent — starts empty
    now,  // Created At
    now   // Updated At
  ]);
}

/**
 * Updates an existing action item by 1-based row index.
 * @param {number} rowIndex
 * @param {Object} data - { title, description, category, dueDate, responsibleIndividuals, status }
 */
function updateTodoItem(rowIndex, data) {
  if (!data.title) throw new Error('Title is required.');
  if (!data.dueDate) throw new Error('Due date is required.');
  const sheet = getOrCreateActionItemsSheet_();
  const now = new Date();
  // Read current row to preserve Reminders Sent and Created At
  const current = sheet.getRange(rowIndex, 1, 1, ACTION_ITEM_HEADERS_.length).getValues()[0];
  sheet.getRange(rowIndex, 1, 1, ACTION_ITEM_HEADERS_.length).setValues([[
    data.title.trim(),
    (data.description || '').trim(),
    data.category || 'Clinic Prep',
    new Date(data.dueDate),
    (data.responsibleIndividuals || ''),
    data.status || 'Not Started',
    current[6],  // Preserve Reminders Sent
    current[7],  // Preserve Created At
    now          // Updated At
  ]]);
}

/**
 * Deletes an action item by 1-based sheet row index.
 * @param {number} rowIndex
 */
function deleteTodoItem(rowIndex) {
  const sheet = getOrCreateActionItemsSheet_();
  sheet.deleteRow(rowIndex);
}
```

- [ ] **Step 2: Push and verify**

```bash
clasp push
```

In Apps Script editor, run `getTodoItems` → confirm it returns `[]` and creates the "Action Items" sheet with the correct headers.

Then run this test snippet in the editor:

```js
function testTodoItemCrud() {
  createTodoItem({ title: 'Test Task', dueDate: '2026-05-01', category: 'Clinic Prep', status: 'Not Started' });
  const items = getTodoItems();
  Logger.log(JSON.stringify(items));  // Should show 1 item
  updateTodoItem(items[0].rowIndex, { title: 'Updated Task', dueDate: '2026-05-02', status: 'In Progress' });
  Logger.log(JSON.stringify(getTodoItems()));  // Should show updated title
  deleteTodoItem(getTodoItems()[0].rowIndex);
  Logger.log(getTodoItems().length);  // Should be 0
}
```

Confirm logs show expected output, then delete `testTodoItemCrud` from the file.

- [ ] **Step 3: Commit**

```bash
git add catbus_scripts/TodoList.gs
git commit -m "feat: add action item CRUD helpers (getTodoItems, createTodoItem, updateTodoItem, deleteTodoItem)"
```

---

## Task 4: Build the Reminder Trigger

**Files:**
- Modify: `catbus_scripts/TodoList.gs`

- [ ] **Step 1: Append reminder functions to `TodoList.gs`**

```js
// ─── Email Reminders ─────────────────────────────────────────────────────────

/**
 * Daily trigger function. Scans all non-Done action items and sends
 * consolidated reminder emails for tasks hitting a threshold today.
 * Run setupTodoReminderTrigger() once to register this as a daily 9 AM trigger.
 */
function sendTaskReminders() {
  const items = getTodoItems().filter(item => item.status !== 'Done');
  if (!items.length) return;

  const coordinators = getCoordinators();
  if (!coordinators.length) {
    Logger.log('sendTaskReminders: no coordinators configured, skipping.');
    return;
  }

  const recipientEmails = coordinators.map(c => c.email).join(', ');

  const today = new Date();
  today.setHours(0, 0, 0, 0);

  // Group tasks by threshold hit today
  const byThreshold = {};  // e.g. { 14: [...], 7: [...], 1: [...] }

  const sheet = getOrCreateActionItemsSheet_();
  const headers = sheet.getRange(1, 1, 1, ACTION_ITEM_HEADERS_.length).getValues()[0];
  const remindersSentColNum = headers.indexOf('Reminders Sent') + 1;

  items.forEach(item => {
    if (!item.dueDate) return;
    const due = new Date(item.dueDate);
    due.setHours(0, 0, 0, 0);
    const daysUntil = Math.round((due - today) / (1000 * 60 * 60 * 24));

    const alreadySent = (item.remindersSent || '').toString().split(',').map(s => parseInt(s.trim(), 10));

    ACTION_ITEM_CONFIG.REMINDER_THRESHOLDS.forEach(threshold => {
      if (daysUntil === threshold && !alreadySent.includes(threshold)) {
        if (!byThreshold[threshold]) byThreshold[threshold] = [];
        byThreshold[threshold].push({ item, daysUntil });
      }
    });
  });

  // Collect all tasks to remind and update Reminders Sent
  const allReminders = [];
  Object.keys(byThreshold).sort((a, b) => a - b).forEach(threshold => {
    byThreshold[threshold].forEach(({ item, daysUntil }) => {
      allReminders.push({ item, daysUntil: parseInt(threshold, 10) });
      // Update Reminders Sent in sheet
      const current = (item.remindersSent || '').toString().trim();
      const updated = current ? current + ',' + threshold : String(threshold);
      sheet.getRange(item.rowIndex, remindersSentColNum).setValue(updated);
    });
  });

  if (!allReminders.length) return;

  // Build consolidated email
  const taskRows = allReminders
    .sort((a, b) => a.daysUntil - b.daysUntil)
    .map(({ item, daysUntil }) => {
      const urgencyLabel = daysUntil === 1 ? '⚠️ Due Tomorrow' : `Due in ${daysUntil} days`;
      return `<tr>
        <td style="padding:8px;border:1px solid #ddd;">${escapeHtmlServer(item.title)}</td>
        <td style="padding:8px;border:1px solid #ddd;">${escapeHtmlServer(item.category)}</td>
        <td style="padding:8px;border:1px solid #ddd;">${escapeHtmlServer(item.dueDate)}</td>
        <td style="padding:8px;border:1px solid #ddd;">${escapeHtmlServer(item.responsibleIndividuals || '—')}</td>
        <td style="padding:8px;border:1px solid #ddd;font-weight:bold;">${urgencyLabel}</td>
      </tr>`;
    }).join('');

  const htmlBody = `
    <h2 style="color:#8e0000;">CATBUS: Upcoming Task Deadlines</h2>
    <p>The following tasks have deadlines approaching:</p>
    <table style="border-collapse:collapse;width:100%;font-family:sans-serif;">
      <thead>
        <tr style="background:#f2f2f2;">
          <th style="padding:8px;border:1px solid #ddd;text-align:left;">Task</th>
          <th style="padding:8px;border:1px solid #ddd;text-align:left;">Category</th>
          <th style="padding:8px;border:1px solid #ddd;text-align:left;">Due Date</th>
          <th style="padding:8px;border:1px solid #ddd;text-align:left;">Responsible</th>
          <th style="padding:8px;border:1px solid #ddd;text-align:left;">Urgency</th>
        </tr>
      </thead>
      <tbody>${taskRows}</tbody>
    </table>
    <p style="color:#666;font-size:0.9em;">This is an automated reminder from CATBUS.</p>
  `;

  sendEmail({
    to: recipientEmails,
    subject: `[CATBUS] Task Reminders — ${allReminders.length} task(s) due soon`,
    htmlBody: htmlBody,
    name: 'CATBUS'
  }, 'sendTaskReminders');

  Logger.log(`sendTaskReminders: sent reminder for ${allReminders.length} task(s) to ${recipientEmails}`);
}

/**
 * One-time setup: registers sendTaskReminders() as a daily 9 AM trigger.
 * Run this once from the Apps Script editor.
 */
function setupTodoReminderTrigger() {
  const existing = ScriptApp.getProjectTriggers().find(
    t => t.getHandlerFunction() === 'sendTaskReminders'
  );
  if (existing) {
    Logger.log('Todo reminder trigger already exists — skipping.');
    return;
  }
  ScriptApp.newTrigger('sendTaskReminders')
    .timeBased()
    .everyDays(1)
    .atHour(9)
    .create();
  Logger.log('Daily todo reminder trigger created — will run at 9 AM each day.');
}
```

- [ ] **Step 2: Push and verify reminder logic manually**

```bash
clasp push
```

In the Action Items sheet, manually add a row with Due Date = tomorrow (today + 1 day), Status = "Not Started", leave Reminders Sent blank. Then in Apps Script editor, run `sendTaskReminders`. Confirm:
1. An email arrives at coordinator addresses with the task listed as "⚠️ Due Tomorrow"
2. The Reminders Sent cell for that row now contains `1`

Run `sendTaskReminders` a second time. Confirm no duplicate email is sent (Reminders Sent already contains `1`).

Clean up the test row from the sheet.

- [ ] **Step 3: Commit**

```bash
git add catbus_scripts/TodoList.gs
git commit -m "feat: add sendTaskReminders and setupTodoReminderTrigger"
```

---

## Task 5: Create Page Server

**Files:**
- Create: `catbus_scripts/TodoListApp.gs`
- Modify: `catbus_scripts/Router.gs`

- [ ] **Step 1: Create `TodoListApp.gs`**

```js
/**
 * TodoListApp.gs
 * Serves the To-Do List page via HtmlService.
 */

/**
 * Serves todo_list.html. Called by Router.gs for ?app=todo.
 */
function doGetTodoList() {
  return loadPage('todo_list', 'To-Do List');
}
```

- [ ] **Step 2: Add route to `Router.gs`**

In `catbus_scripts/Router.gs`, inside the `switch (app)` block, add before the `default:` case:

```js
case 'todo':
  return doGetTodoList();
```

- [ ] **Step 3: Push and verify routing**

```bash
clasp push
```

Navigate to `<WEBAPP_URL>?app=todo` in a browser. Confirm the page loads (it will be blank/error until the HTML file is created — a "Script function not found" or blank page is expected at this stage; what you're checking is that no router crash occurs and the function is called).

- [ ] **Step 4: Commit**

```bash
git add catbus_scripts/TodoListApp.gs catbus_scripts/Router.gs
git commit -m "feat: add TodoListApp page server and router entry for ?app=todo"
```

---

## Task 6: Build the To-Do List Dashboard HTML

**Files:**
- Create: `catbus_scripts/todo_list.html`

- [ ] **Step 1: Create `catbus_scripts/todo_list.html` with full dashboard**

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <base target="_top">
  <style>
    :root {
      --accent:  rgb(142, 0, 0);
      --bg:      #ffffff;
      --text:    #1a1a1a;
      --card-bg: #f2f2f2;
      --border:  #ccc;
      --red:     #e74c3c;
      --orange:  #e67e22;
      --yellow:  #f1c40f;
      --green:   #27ae60;
    }
    body { margin: 0 auto; font-family: 'Segoe UI', Verdana, sans-serif; padding: 20px; max-width: 1100px; background: var(--bg); color: var(--text); }
    h1 { margin: 0 0 4px; color: var(--accent); }
    .subtitle { margin: 0 0 20px; color: #666; font-size: 0.9rem; }

    /* ── Toolbar ── */
    .toolbar { display: flex; gap: 10px; align-items: center; flex-wrap: wrap; margin-bottom: 16px; }
    .filter-group { display: flex; gap: 6px; flex-wrap: wrap; }
    .filter-btn { padding: 6px 14px; border: 1px solid var(--border); border-radius: 20px; background: #fff; cursor: pointer; font-size: 0.85rem; }
    .filter-btn.active { background: var(--accent); color: #fff; border-color: var(--accent); }
    .btn-add { margin-left: auto; padding: 8px 18px; background: var(--accent); color: #fff; border: none; border-radius: 4px; font-size: 0.95rem; font-weight: 600; cursor: pointer; }
    .btn-add:hover { opacity: 0.85; }

    /* ── Table ── */
    table { width: 100%; border-collapse: collapse; font-size: 0.9rem; }
    thead th { background: var(--card-bg); padding: 10px 12px; text-align: left; border-bottom: 2px solid var(--border); font-weight: 600; }
    tbody td { padding: 10px 12px; border-bottom: 1px solid var(--border); vertical-align: top; }
    tbody tr:hover { background: #fafafa; }
    tbody tr.urgency-red   { border-left: 4px solid var(--red); }
    tbody tr.urgency-orange { border-left: 4px solid var(--orange); }
    tbody tr.urgency-yellow { border-left: 4px solid var(--yellow); }
    tbody tr.urgency-green  { border-left: 4px solid var(--green); }
    .badge { display: inline-block; padding: 2px 8px; border-radius: 12px; font-size: 0.78rem; font-weight: 600; }
    .badge-not-started { background: #eee; color: #555; }
    .badge-in-progress { background: #dbeafe; color: #1d4ed8; }
    .badge-done        { background: #dcfce7; color: #166534; }
    .days-chip { font-weight: 700; }
    .days-overdue { color: var(--red); }
    .days-soon    { color: var(--orange); }
    .days-warning { color: #b58900; }
    .days-ok      { color: var(--green); }
    .link-edit { color: var(--accent); text-decoration: underline; cursor: pointer; font-size: 0.85rem; }
    .empty-state { text-align: center; padding: 40px; color: #999; }

    /* ── Manage People section ── */
    .people-toggle { display: flex; align-items: center; gap: 8px; margin-top: 28px; cursor: pointer; user-select: none; font-weight: 600; color: var(--accent); border: none; background: none; font-size: 1rem; padding: 0; }
    .people-section { display: none; margin-top: 12px; background: var(--card-bg); border: 1px solid var(--border); border-radius: 8px; padding: 18px; }
    .people-section.open { display: block; }
    .people-section h3 { margin: 0 0 12px; font-size: 1rem; }
    .people-table { width: 100%; border-collapse: collapse; font-size: 0.88rem; margin-bottom: 16px; }
    .people-table th, .people-table td { padding: 8px 10px; border-bottom: 1px solid var(--border); text-align: left; }
    .people-table th { font-weight: 600; background: #e8e8e8; }
    .btn-delete { padding: 4px 10px; background: #e74c3c; color: #fff; border: none; border-radius: 4px; cursor: pointer; font-size: 0.8rem; }
    .btn-delete:hover { opacity: 0.85; }
    .add-person-form { display: flex; gap: 10px; flex-wrap: wrap; align-items: flex-end; }
    .add-person-form input { padding: 8px 10px; border: 1px solid var(--border); border-radius: 4px; font-size: 0.9rem; flex: 1; min-width: 140px; }
    .btn-add-person { padding: 8px 16px; background: #27ae60; color: #fff; border: none; border-radius: 4px; font-weight: 600; cursor: pointer; font-size: 0.9rem; white-space: nowrap; }
    .btn-add-person:hover { opacity: 0.85; }

    /* ── Modal ── */
    .modal-overlay { display: none; position: fixed; inset: 0; background: rgba(0,0,0,0.45); z-index: 100; align-items: center; justify-content: center; }
    .modal-overlay.open { display: flex; }
    .modal { background: #fff; border-radius: 8px; padding: 28px; width: 100%; max-width: 520px; box-shadow: 0 8px 32px rgba(0,0,0,0.18); }
    .modal h2 { margin: 0 0 20px; font-size: 1.2rem; color: var(--accent); }
    .form-group { margin-bottom: 16px; }
    .form-group label { display: block; font-weight: 600; margin-bottom: 6px; font-size: 0.9rem; }
    .form-group input, .form-group textarea, .form-group select { width: 100%; box-sizing: border-box; padding: 9px 11px; border: 1px solid var(--border); border-radius: 4px; font-size: 0.9rem; font-family: inherit; }
    .form-group textarea { resize: vertical; min-height: 70px; }
    .checkboxes { display: flex; flex-wrap: wrap; gap: 8px; }
    .checkboxes label { font-weight: normal; display: flex; align-items: center; gap: 5px; font-size: 0.88rem; cursor: pointer; }
    .modal-actions { display: flex; gap: 10px; margin-top: 22px; justify-content: flex-end; }
    .btn-save { padding: 9px 22px; background: var(--accent); color: #fff; border: none; border-radius: 4px; font-weight: 600; cursor: pointer; }
    .btn-cancel { padding: 9px 22px; background: #eee; color: #333; border: none; border-radius: 4px; font-weight: 600; cursor: pointer; }
    .btn-delete-task { padding: 9px 18px; background: var(--red); color: #fff; border: none; border-radius: 4px; font-weight: 600; cursor: pointer; margin-right: auto; }
    .status-msg { font-size: 0.85rem; margin-top: 8px; min-height: 18px; }
    .status-msg.error { color: var(--red); }
    .status-msg.success { color: var(--green); }
  </style>
</head>
<body>

<h1>To-Do List</h1>
<p class="subtitle">Clinic action items and deadlines for coordinators.</p>

<!-- Toolbar -->
<div class="toolbar">
  <div class="filter-group" id="catFilter">
    <button class="filter-btn active" data-cat="All" onclick="setFilter('cat','All',this)">All Categories</button>
    <button class="filter-btn" data-cat="Clinic Prep" onclick="setFilter('cat','Clinic Prep',this)">Clinic Prep</button>
    <button class="filter-btn" data-cat="Volunteer Management" onclick="setFilter('cat','Volunteer Management',this)">Volunteer Management</button>
  </div>
  <div class="filter-group" id="statusFilter">
    <button class="filter-btn active" data-status="All" onclick="setFilter('status','All',this)">All Statuses</button>
    <button class="filter-btn" data-status="Not Started" onclick="setFilter('status','Not Started',this)">Not Started</button>
    <button class="filter-btn" data-status="In Progress" onclick="setFilter('status','In Progress',this)">In Progress</button>
    <button class="filter-btn" data-status="Done" onclick="setFilter('status','Done',this)">Done</button>
  </div>
  <button class="btn-add" onclick="openAddModal()">+ Add Task</button>
</div>

<!-- Task Table -->
<table id="taskTable">
  <thead>
    <tr>
      <th>Title</th>
      <th>Category</th>
      <th>Due Date</th>
      <th>Responsible</th>
      <th>Status</th>
      <th>Days Until Due</th>
      <th></th>
    </tr>
  </thead>
  <tbody id="taskTableBody">
    <tr><td colspan="7" class="empty-state">Loading…</td></tr>
  </tbody>
</table>

<!-- Manage People Toggle -->
<button class="people-toggle" onclick="togglePeople()">
  <span id="peopleArrow">▶</span> Manage People
</button>
<div class="people-section" id="peopleSection">
  <h3>Coordinators</h3>
  <table class="people-table">
    <thead><tr><th>Name</th><th>Email</th><th></th></tr></thead>
    <tbody id="peopleTableBody"><tr><td colspan="3">Loading…</td></tr></tbody>
  </table>
  <div class="add-person-form">
    <input type="text" id="newPersonName" placeholder="Name" />
    <input type="email" id="newPersonEmail" placeholder="Email" />
    <button class="btn-add-person" onclick="addPerson()">Add Person</button>
  </div>
  <div class="status-msg" id="peopleStatus"></div>
</div>

<!-- Task Modal -->
<div class="modal-overlay" id="taskModal">
  <div class="modal">
    <h2 id="modalTitle">Add Task</h2>
    <div class="form-group">
      <label>Title *</label>
      <input type="text" id="fTitle" />
    </div>
    <div class="form-group">
      <label>Description</label>
      <textarea id="fDesc"></textarea>
    </div>
    <div class="form-group">
      <label>Category</label>
      <select id="fCategory">
        <option>Clinic Prep</option>
        <option>Volunteer Management</option>
      </select>
    </div>
    <div class="form-group">
      <label>Due Date *</label>
      <input type="date" id="fDueDate" />
    </div>
    <div class="form-group">
      <label>Responsible Individuals</label>
      <div class="checkboxes" id="fResponsible"></div>
    </div>
    <div class="form-group">
      <label>Status</label>
      <select id="fStatus">
        <option>Not Started</option>
        <option>In Progress</option>
        <option>Done</option>
      </select>
    </div>
    <div class="status-msg" id="modalStatus"></div>
    <div class="modal-actions">
      <button class="btn-delete-task" id="btnDelete" style="display:none" onclick="deleteTask()">Delete</button>
      <button class="btn-cancel" onclick="closeModal()">Cancel</button>
      <button class="btn-save" onclick="saveTask()">Save</button>
    </div>
  </div>
</div>

<script>
  var allItems = [];
  var coordinators = [];
  var editingRowIndex = null;
  var filterCat = 'All';
  var filterStatus = 'All';

  // ── Init ──────────────────────────────────────────────────────────────────
  function init() {
    loadCoordinators();
    loadTasks();
  }

  function loadTasks() {
    google.script.run
      .withSuccessHandler(function(items) {
        allItems = items;
        renderTable();
      })
      .withFailureHandler(function(err) {
        document.getElementById('taskTableBody').innerHTML =
          '<tr><td colspan="7" class="empty-state" style="color:#e74c3c;">Error loading tasks: ' + err.message + '</td></tr>';
      })
      .getTodoItems();
  }

  function loadCoordinators() {
    google.script.run
      .withSuccessHandler(function(list) {
        coordinators = list;
        renderPeople();
      })
      .withFailureHandler(function(err) {
        document.getElementById('peopleStatus').textContent = 'Error loading coordinators: ' + err.message;
        document.getElementById('peopleStatus').className = 'status-msg error';
      })
      .getCoordinators();
  }

  // ── Table Rendering ───────────────────────────────────────────────────────
  function renderTable() {
    var tbody = document.getElementById('taskTableBody');
    var filtered = allItems.filter(function(item) {
      var catOk = filterCat === 'All' || item.category === filterCat;
      var statusOk = filterStatus === 'All' || item.status === filterStatus;
      return catOk && statusOk;
    });

    if (!filtered.length) {
      tbody.innerHTML = '<tr><td colspan="7" class="empty-state">No tasks found.</td></tr>';
      return;
    }

    // Sort: non-done by daysUntil ascending, done last
    filtered.sort(function(a, b) {
      if (a.status === 'Done' && b.status !== 'Done') return 1;
      if (b.status === 'Done' && a.status !== 'Done') return -1;
      return daysUntil(a.dueDate) - daysUntil(b.dueDate);
    });

    tbody.innerHTML = filtered.map(function(item) {
      var days = daysUntil(item.dueDate);
      var urgencyClass = urgency(item, days);
      var daysLabel = daysLabel_(item, days);
      var badgeClass = { 'Not Started': 'badge-not-started', 'In Progress': 'badge-in-progress', 'Done': 'badge-done' }[item.status] || 'badge-not-started';
      return '<tr class="' + urgencyClass + '">' +
        '<td>' + esc(item.title) + '</td>' +
        '<td>' + esc(item.category) + '</td>' +
        '<td>' + esc(item.dueDate) + '</td>' +
        '<td>' + esc(item.responsibleIndividuals || '—') + '</td>' +
        '<td><span class="badge ' + badgeClass + '">' + esc(item.status) + '</span></td>' +
        '<td><span class="days-chip ' + daysColorClass(item, days) + '">' + daysLabel + '</span></td>' +
        '<td><span class="link-edit" onclick="openEditModal(' + item.rowIndex + ')">Edit</span></td>' +
        '</tr>';
    }).join('');
  }

  function daysUntil(dateStr) {
    if (!dateStr) return 9999;
    var today = new Date(); today.setHours(0,0,0,0);
    var due = new Date(dateStr); due.setHours(0,0,0,0);
    return Math.round((due - today) / 86400000);
  }

  function urgency(item, days) {
    if (item.status === 'Done') return 'urgency-green';
    if (days <= 1) return 'urgency-red';
    if (days <= 7) return 'urgency-orange';
    if (days <= 14) return 'urgency-yellow';
    return 'urgency-green';
  }

  function daysColorClass(item, days) {
    if (item.status === 'Done') return 'days-ok';
    if (days < 0) return 'days-overdue';
    if (days <= 1) return 'days-overdue';
    if (days <= 7) return 'days-soon';
    if (days <= 14) return 'days-warning';
    return 'days-ok';
  }

  function daysLabel_(item, days) {
    if (item.status === 'Done') return 'Done';
    if (days < 0) return 'Overdue (' + Math.abs(days) + 'd)';
    if (days === 0) return 'Due Today';
    if (days === 1) return 'Due Tomorrow';
    return days + ' days';
  }

  // ── Filters ───────────────────────────────────────────────────────────────
  function setFilter(type, value, btn) {
    if (type === 'cat') {
      filterCat = value;
      document.querySelectorAll('#catFilter .filter-btn').forEach(function(b) { b.classList.remove('active'); });
    } else {
      filterStatus = value;
      document.querySelectorAll('#statusFilter .filter-btn').forEach(function(b) { b.classList.remove('active'); });
    }
    btn.classList.add('active');
    renderTable();
  }

  // ── Modal ─────────────────────────────────────────────────────────────────
  function openAddModal() {
    editingRowIndex = null;
    document.getElementById('modalTitle').textContent = 'Add Task';
    document.getElementById('fTitle').value = '';
    document.getElementById('fDesc').value = '';
    document.getElementById('fCategory').value = 'Clinic Prep';
    document.getElementById('fDueDate').value = '';
    document.getElementById('fStatus').value = 'Not Started';
    document.getElementById('btnDelete').style.display = 'none';
    document.getElementById('modalStatus').textContent = '';
    renderResponsibleCheckboxes([]);
    document.getElementById('taskModal').classList.add('open');
  }

  function openEditModal(rowIndex) {
    var item = allItems.find(function(i) { return i.rowIndex === rowIndex; });
    if (!item) return;
    editingRowIndex = rowIndex;
    document.getElementById('modalTitle').textContent = 'Edit Task';
    document.getElementById('fTitle').value = item.title;
    document.getElementById('fDesc').value = item.description || '';
    document.getElementById('fCategory').value = item.category;
    document.getElementById('fDueDate').value = item.dueDate;
    document.getElementById('fStatus').value = item.status;
    document.getElementById('btnDelete').style.display = 'inline-block';
    document.getElementById('modalStatus').textContent = '';
    var selected = (item.responsibleIndividuals || '').split(',').map(function(s) { return s.trim(); });
    renderResponsibleCheckboxes(selected);
    document.getElementById('taskModal').classList.add('open');
  }

  function renderResponsibleCheckboxes(selected) {
    var container = document.getElementById('fResponsible');
    if (!coordinators.length) {
      container.innerHTML = '<span style="color:#999;font-size:0.85rem;">No coordinators configured. Add people below.</span>';
      return;
    }
    container.innerHTML = coordinators.map(function(c) {
      var checked = selected.indexOf(c.name) >= 0 ? ' checked' : '';
      return '<label><input type="checkbox" value="' + esc(c.name) + '"' + checked + '> ' + esc(c.name) + '</label>';
    }).join('');
  }

  function closeModal() {
    document.getElementById('taskModal').classList.remove('open');
  }

  function getFormData() {
    var checked = Array.from(document.querySelectorAll('#fResponsible input:checked')).map(function(cb) { return cb.value; });
    return {
      title:                  document.getElementById('fTitle').value.trim(),
      description:            document.getElementById('fDesc').value.trim(),
      category:               document.getElementById('fCategory').value,
      dueDate:                document.getElementById('fDueDate').value,
      responsibleIndividuals: checked.join(', '),
      status:                 document.getElementById('fStatus').value
    };
  }

  function saveTask() {
    var data = getFormData();
    var statusEl = document.getElementById('modalStatus');
    if (!data.title) { statusEl.textContent = 'Title is required.'; statusEl.className = 'status-msg error'; return; }
    if (!data.dueDate) { statusEl.textContent = 'Due date is required.'; statusEl.className = 'status-msg error'; return; }
    statusEl.textContent = 'Saving…'; statusEl.className = 'status-msg';

    if (editingRowIndex === null) {
      google.script.run
        .withSuccessHandler(function() { closeModal(); loadTasks(); })
        .withFailureHandler(function(err) { statusEl.textContent = 'Error: ' + err.message; statusEl.className = 'status-msg error'; })
        .createTodoItem(data);
    } else {
      google.script.run
        .withSuccessHandler(function() { closeModal(); loadTasks(); })
        .withFailureHandler(function(err) { statusEl.textContent = 'Error: ' + err.message; statusEl.className = 'status-msg error'; })
        .updateTodoItem(editingRowIndex, data);
    }
  }

  function deleteTask() {
    if (!editingRowIndex) return;
    if (!confirm('Delete this task? This cannot be undone.')) return;
    google.script.run
      .withSuccessHandler(function() { closeModal(); loadTasks(); })
      .withFailureHandler(function(err) { document.getElementById('modalStatus').textContent = 'Error: ' + err.message; })
      .deleteTodoItem(editingRowIndex);
  }

  // ── Manage People ─────────────────────────────────────────────────────────
  function togglePeople() {
    var section = document.getElementById('peopleSection');
    var arrow = document.getElementById('peopleArrow');
    var isOpen = section.classList.toggle('open');
    arrow.textContent = isOpen ? '▼' : '▶';
  }

  function renderPeople() {
    var tbody = document.getElementById('peopleTableBody');
    if (!coordinators.length) {
      tbody.innerHTML = '<tr><td colspan="3" style="color:#999;">No coordinators yet.</td></tr>';
      return;
    }
    tbody.innerHTML = coordinators.map(function(c) {
      return '<tr>' +
        '<td>' + esc(c.name) + '</td>' +
        '<td>' + esc(c.email) + '</td>' +
        '<td><button class="btn-delete" onclick="deletePerson(' + c.rowIndex + ')">Remove</button></td>' +
        '</tr>';
    }).join('');
  }

  function addPerson() {
    var name = document.getElementById('newPersonName').value.trim();
    var email = document.getElementById('newPersonEmail').value.trim();
    var statusEl = document.getElementById('peopleStatus');
    if (!name || !email) { statusEl.textContent = 'Name and email are required.'; statusEl.className = 'status-msg error'; return; }
    statusEl.textContent = 'Adding…'; statusEl.className = 'status-msg';
    google.script.run
      .withSuccessHandler(function() {
        document.getElementById('newPersonName').value = '';
        document.getElementById('newPersonEmail').value = '';
        statusEl.textContent = 'Added.'; statusEl.className = 'status-msg success';
        loadCoordinators();
      })
      .withFailureHandler(function(err) { statusEl.textContent = 'Error: ' + err.message; statusEl.className = 'status-msg error'; })
      .addCoordinator({ name: name, email: email });
  }

  function deletePerson(rowIndex) {
    if (!confirm('Remove this coordinator?')) return;
    google.script.run
      .withSuccessHandler(function() { loadCoordinators(); })
      .withFailureHandler(function(err) { document.getElementById('peopleStatus').textContent = 'Error: ' + err.message; })
      .deleteCoordinator(rowIndex);
  }

  // ── Utilities ─────────────────────────────────────────────────────────────
  function esc(str) {
    return String(str || '').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;');
  }

  // Close modal on overlay click
  document.getElementById('taskModal').addEventListener('click', function(e) {
    if (e.target === this) closeModal();
  });

  init();
</script>
</body>
</html>
```

- [ ] **Step 2: Push and do full end-to-end test**

```bash
clasp push
```

Navigate to `<WEBAPP_URL>?app=todo`. Verify:
1. Page loads with empty task table
2. "Add Task" opens modal; fill all fields and save → task appears in table and in the "Action Items" sheet
3. Click "Edit" on the task → modal pre-fills; change status to "In Progress", save → table updates
4. Urgency color coding: set due date to tomorrow → red left border; 5 days out → orange; 10 days → yellow; 30 days → green
5. Filter buttons hide/show tasks correctly
6. "Manage People" toggle expands/collapses the section
7. Add a coordinator via the form → appears in table and in "Coordinators" sheet
8. Remove a coordinator → row disappears
9. Open "Add Task" modal after adding a coordinator → coordinator appears as a checkbox
10. Delete a task via Edit modal → row removed from sheet

- [ ] **Step 3: Commit**

```bash
git add catbus_scripts/todo_list.html
git commit -m "feat: add To-Do List frontend with task CRUD, filters, and Manage People section"
```

---

## Task 7: Add Admin Page Tile

**Files:**
- Modify: `webpage/admin.html`

- [ ] **Step 1: Add the To-Do List tile to `webpage/admin.html`**

Open `webpage/admin.html`. Find the last `<a class="tool-card"...>` block and add a new tile after it (before any closing `</div>` or `</section>` that wraps the cards):

```html
<a class="tool-card" href="https://script.google.com/macros/s/AKfycbxoTK4G2ZaGCUGZQBeWiw2BuoFkGOh7SaLlL2EgRhhhe4slTKMJ4SmH29q8On0-Fmlw2A/exec?app=todo">
    <div class="tool-card-title">To-Do List</div>
    <p class="tool-card-desc">Track key action items and deadlines for clinic coordinators.</p>
</a>
```

- [ ] **Step 2: Open `webpage/admin.html` in a browser and verify**

The new "To-Do List" tile should appear in the admin tools grid. Clicking it should navigate to `?app=todo`.

- [ ] **Step 3: Commit**

```bash
git add webpage/admin.html
git commit -m "feat: add To-Do List tile to admin page"
```

---

## Task 8: Register the Reminder Trigger

- [ ] **Step 1: Run `setupTodoReminderTrigger()` once**

In the Apps Script editor (script.google.com), open the project, select `setupTodoReminderTrigger` from the function dropdown, and click Run.

Confirm in the Apps Script **Triggers** panel (⏰ icon in left sidebar) that a new daily trigger for `sendTaskReminders` appears, set to run between 9–10 AM.

- [ ] **Step 2: Done — no commit needed**

The trigger is registered server-side in Apps Script and does not live in source code.

---

## Verification Checklist

End-to-end smoke test after all tasks are complete:

- [ ] `?app=todo` loads without errors
- [ ] Add a task (Clinic Prep, due in 3 days, assign a coordinator) → appears in table with orange urgency
- [ ] Edit task → changes persist
- [ ] Delete task → removed from sheet
- [ ] Add coordinator → appears in Manage People table and in task form checkboxes
- [ ] Remove coordinator → disappears from both
- [ ] Set a task due tomorrow, manually run `sendTaskReminders()` → reminder email received with task listed; Reminders Sent cell updated to `1`
- [ ] Run `sendTaskReminders()` again → no second email sent
- [ ] Admin page tile navigates to `?app=todo`
- [ ] Filter by category and status work correctly
