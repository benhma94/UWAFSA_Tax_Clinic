# Claude Code session data (session-specific, not shared)
.claude/*
!.claude/settings.json

# Secrets — instance-specific values, never commit
catbus_scripts/_Secrets.gs
catbus_scripts/.clasp.json

# Webpage runtime config — upload to server manually, never commit
webpage/config.js

# Graphify generated outputs (large/binary-like, rebuilt on demand)
.graphify_*

# Superpowers skills/plugins (local tooling, not shared)
.superpowers/

# Obsidian vaults (personal notes, not shared)
Obsidian - Tax Clinic Website/